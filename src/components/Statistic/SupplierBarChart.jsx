import React from 'react';
import ChartComponent from './ChartComponent';

const SupplierBarChart = ({ data }) => {
    return (
        <ChartComponent
            data={data}
        />
    );
};

export default SupplierBarChart;
